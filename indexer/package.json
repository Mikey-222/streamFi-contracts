{
  "name": "streamfi-indexer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start:worker": "tsx src/worker.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "pg-mem": "^2.8.1",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0",
    "vitest": "^1.6.0"
  }
}
